import React from 'react';

const NestedRoutes = () => {
    return (
        <div>
            <h1 className="text-xl font-bold">loo at the url in default its "/home" but  by this thing you can create page inside of the main page making url like "/home/(the name you want)"</h1>
        </div>
    );
};

export default NestedRoutes;